use std::cmp::{max, min};
use std::collections::{HashMap, HashSet, VecDeque};

use utils::{direction, GridPosition, GridPositionAndDirection, Position};


pub(crate) fn run(a:u32,b:u32,c:u32,program:Vec<u32>) -> i32{
    let mut result:i32 = i32::MAX;
    result
}

